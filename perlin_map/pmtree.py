# A kd tree-based representation

class Cell:
    # 4-arrays are
    def __init__(self, grads, children, depth):
        self.grads = grads
        self.children = children

class PMTree:
    def __init__(self, pmap):
        for i in range(pmap.size):
            print("deez nuts")

